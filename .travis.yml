sudo: required

services:
  - docker

install:
  - docker build -t docker-jenkins-config:travis .
  - docker run --name jenkins --env-file config/test-env.list docker-jenkins-config:travis

script:
 - docker ps | grep -q jenkins
